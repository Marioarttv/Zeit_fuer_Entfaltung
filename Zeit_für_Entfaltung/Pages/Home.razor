@page "/"
@using Microsoft.AspNetCore.Components.Web

<PageTitle>Zeit für Entfaltung - Persönliche Begleitung für mehr Lebensfreude</PageTitle>

<div class="hero-section">
    <div class="hero-background"></div>
    <div class="hero-content container">
        <div class="logo-container">
            <img src="images/Logo-e7548854-1920w.webp" alt="Zeit für Entfaltung" class="hero-logo animate-float" />
        </div>
        <h1 class="animate-fadeInUp" style="animation-delay: 0.3s">Zeit für Entfaltung</h1>
        <p class="hero-tagline animate-fadeInUp" style="animation-delay: 0.5s">Entdecke dein wahres Potenzial</p>
    </div>
    <div class="hero-wave">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320">
            <path fill="#ffffff" fill-opacity="1" d="M0,192L48,197.3C96,203,192,213,288,208C384,203,480,181,576,181.3C672,181,768,203,864,218.7C960,235,1056,245,1152,234.7C1248,224,1344,192,1392,176L1440,160L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path>
        </svg>
    </div>
</div>

<section class="welcome-section section">
    <div class="container">
        <div class="welcome-card animate-scaleIn">
            <h2 class="welcome-heading">Herzlich Willkommen bei Zeit für Entfaltung</h2>
            
            <div class="welcome-quote">
                <p>Ich freue mich sehr, dass du hier bist.</p>
            </div>
            
            <p class="main-text">Mein Herzensanliegen ist es, Menschen wie dich auf ihrem persönlichen Weg zu begleiten. Gemeinsam entdecken wir dein wahres Potenzial und schaffen Raum für mehr Lebensfreude und Gelassenheit.</p>
            
            <p class="main-text">Auf den folgenden Seiten findest du mehr über meine Arbeit und die verschiedenen Wege, wie ich dich auf deinem individuellen Weg unterstützen kann.</p>
            
            <div class="cta-container">
                <a href="kontakt" class="btn">Kontakt aufnehmen</a>
            </div>
        </div>
    </div>
</section>

<style>
    /* Import our theme CSS variables using link tag instead */
    /* CSS variables will be imported via the link tag */
    
    /* Hero Section */
    .hero-section {
        position: relative;
        min-height: 85vh;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        background-color: var(--light-green);
        padding: 0;
    }
    
    .hero-background {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background-image: 
            radial-gradient(circle at 20% 30%, rgba(131, 167, 143, 0.2) 0%, transparent 30%),
            radial-gradient(circle at 80% 70%, rgba(131, 167, 143, 0.2) 0%, transparent 30%);
        opacity: 0.8;
        z-index: 1;
    }
    
    .hero-content {
        position: relative;
        z-index: 2;
        text-align: center;
        padding: var(--space-xl) 0;
    }
    
    .logo-container {
        margin-bottom: var(--space-lg);
    }
    
    .hero-logo {
        max-width: 300px;
        height: auto;
        border-radius: var(--radius-md);
        box-shadow: var(--shadow-lg);
        transition: var(--transition-medium);
    }
    
    .animate-float {
        animation: float 6s ease-in-out infinite;
    }
    
    .hero-section h1 {
        font-size: 3.5rem;
        color: var(--primary-green);
        margin-bottom: var(--space-sm);
        font-weight: 700;
    }
    
    .hero-tagline {
        font-size: 1.5rem;
        color: var(--accent-green);
        margin-bottom: 95px;
        font-weight: 300;
        font-family: var(--quote-font);
        font-style: italic;
    }
    
    .hero-wave {
        position: absolute;
        bottom: -2px; /* Ensure wave connects properly with section below */
        left: 0;
        width: 100%;
        line-height: 0;
        z-index: 3;
    }
    
    /* Welcome Section */
    .welcome-section {
        position: relative;
        background-color: white;
    }
    
    .welcome-card {
        background-color: white;
        border-radius: var(--radius-lg);
        box-shadow: var(--shadow-md);
        padding: var(--space-lg);
        position: relative;
        max-width: 900px;
        margin: 0 auto;
        border-top: 4px solid var(--secondary-green);
    }
    
    .welcome-heading {
        text-align: center;
        margin-bottom: var(--space-md);
        color: var(--primary-green);
        font-weight: 600;
    }
    
    .welcome-quote {
        background-color: var(--very-light-green);
        border-radius: var(--radius-md);
        padding: var(--space-md);
        margin-bottom: var(--space-md);
        position: relative;
        text-align: center;
    }
    
    .welcome-quote p {
        font-family: var(--quote-font);
        font-size: 1.5rem;
        color: var(--primary-green);
        font-style: italic;
        margin: 0;
    }
    
    .main-text {
        font-size: 1.15rem;
        line-height: 1.7;
        margin-bottom: var(--space-md);
        color: var(--corduroy);
    }
    
    .cta-container {
        text-align: center;
        margin-top: var(--space-lg);
    }
    
    /* Animation Classes */
    .animate-fadeIn {
        opacity: 0;
        animation: fadeIn var(--transition-slow) forwards;
    }
    
    .animate-fadeInUp {
        opacity: 0;
        animation: fadeInUp var(--transition-slow) forwards;
    }
    
    .animate-scaleIn {
        opacity: 0;
        animation: scaleIn var(--transition-slow) forwards;
        animation-delay: 0.3s;
    }
    
    /* Responsive Adjustments */
    @@media (max-width: 992px) {
        .hero-section {
            min-height: 75vh;
        }
        
        .hero-section h1 {
            font-size: 3rem;
        }
        
        .hero-tagline {
            font-size: 1.3rem;
        }
    }
    
    @@media (max-width: 768px) {
        .hero-section {
            min-height: 60vh;
        }
        
        .hero-section h1 {
            font-size: 2.5rem;
        }
        
        .welcome-quote p {
            font-size: 1.4rem;
        }
        
        .welcome-card {
            padding: var(--space-md);
        }
        
        .main-text {
            font-size: 1.1rem;
        }
    }
    
    @@media (max-width: 576px) {
        .hero-logo {
            max-width: 200px;
        }
        
        .hero-section h1 {
            font-size: 2rem;
        }
        
        .hero-tagline {
            font-size: 1.1rem;
        }
        
        .welcome-quote p {
            font-size: 1.3rem;
        }
        
        .welcome-heading {
            font-size: 1.8rem;
        }
        
        .main-text {
            font-size: 1.05rem;
        }
    }
</style>

@code {
    // You can add any required code here
}